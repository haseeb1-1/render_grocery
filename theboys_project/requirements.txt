# Core Flask
flask==2.3.2
gunicorn==20.1.0  # Production server

# Database
flask-sqlalchemy==3.0.3  # ORM for SQLite/PostgreSQL
psycopg2-binary==2.9.6   # PostgreSQL adapter (if using Render's PostgreSQL)

# Authentication
flask-login==0.6.2       # User sessions
werkzeug==2.3.6          # Password hashing

# Forms & Security
flask-wtf==1.0.0         # Form handling
wtforms==3.0.1           # Form validation
python-dotenv==1.0.0     # Environment variables

# Frontend (if using Bootstrap)
flask-bootstrap==3.3.7.1

# Payments (optional)
stripe==5.5.0            # For payment gateway


flask-mail==0.9.1
Pillow==9.5.0
flask-restful==0.3.9